{
  "name": "batch-react",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/billing_projects.jsx --bundle --jsx=automatic --format=esm --splitting --outdir=../static/compiled-js --minify"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
